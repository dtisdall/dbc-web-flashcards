<% if session[:prevous] == true %>
<p>YAYAYAYAYAYYY</p>
<% else %>
<p> LEAARRNNNNN!!</p>
<% end %>

<%= @card.question %>
<form method="post" action=<%= "/round/#{@round.id}/check_guess" %>>
  <input type="hidden" name="card_id" value=<%= "#{@card.id}" %>>
  <input type="text" name="guess">
  <button type="submit" class="btn btn-default">Submit</button>
</form>
